[
  {
    "namespace": "telemetry",
    "description": "Use the browser.telemetry API to send telemetry data to a telemetry service.",
  
   "permissions": ["experiments.telemetry"],
   "properties": {
     "network": {
       "type": "object"
     }
   }
  }
]
